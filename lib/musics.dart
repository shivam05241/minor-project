class MusicData {
  String musicName, artistName, image, tag, url, id;

  MusicData({
    this.id,
    this.musicName,
    this.artistName,
    this.image,
    this.tag,
    this.url,
  });
}

final List<MusicData> list = [];

// List<MusicData> favList = [];
